spring.application.name=FlexResume

# 数据库配置 - 使用环境变量
spring.data.mongodb.uri=${MONGODB_URI:mongodb://resumedb:Wangzhaoyu011207@60.205.155.237:27017/resume_builder?authSource=admin}

# Redis 缓存配置
spring.data.redis.host=${REDIS_HOST:redis}
spring.data.redis.port=${REDIS_PORT:6379}
spring.data.redis.timeout=2000ms
spring.data.redis.lettuce.pool.max-active=8
spring.data.redis.lettuce.pool.max-idle=8
spring.data.redis.lettuce.pool.min-idle=0

# 缓存配置
spring.cache.type=redis
spring.cache.redis.time-to-live=300000
spring.cache.redis.cache-null-values=false

logging.level.org.springframework=INFO

# 服务器端口配置 - 使用环境变量
server.port=${SERVER_PORT:8081}

# CORS配置相关的环境变量
cors.allowed.origins=${FRONTEND_URL:http://60.205.155.237:8080},${DEV_FRONTEND_URL:http://localhost:3000},${DOMAIN_URL:http://www.flexresume.me},https://60.205.155.237,https://www.flexresume.me,https://flexresume.me,http://localhost:3000

# 文件上传配置
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# 服务器配置
server.max-http-header-size=1MB

